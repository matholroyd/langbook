#user_bar.site
  - if current_user
    = link_to 'decks', user_decks_path(current_user)
    = link_to 'account', user_path(current_user)
    = link_to 'log out', user_session_path, :method => :delete
  - else
    = link_to 'log in', new_user_session_path, :class => 'main'
    = link_to 'sign up', new_user_path
