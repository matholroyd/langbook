:ruby
  heading 'New Card' do |back, forward|
    back.title = 'Back'
    back.path = deck_path(@deck)
  end

%h2 Creating card

= error_messages_for :card

- semantic_form_for [@deck, @card] do |f|
  = render :partial => "form", :object => f, :locals => {:cancel_link => deck_path(@deck)}

